@model Articles.Models.ArticlesComponentModel

<div class="container">
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-6 gap-4">
        @foreach (var article in Model.Articles)
        {
            @Html.Partial("/Views/Components/Partials/ArticleTile.cshtml", article)
        }
    </div>
</div>
